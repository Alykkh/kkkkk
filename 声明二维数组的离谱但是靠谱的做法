首先，我们在一维数组里面定义数组可以这样来做：
 int[] arr;
 int []arr;


 其次呢我们在二维数组中可以使用这样的：
 int[][]arr;
 int arr[][];
 int []arr[];

 所以综上所述，我们可以有这样的做法：
 int []x,y[];
 表示说x是一个一维数组，y是一个二维数组。
 
 那么根据上述来看一些练习，以便帮助大家更好的理解二维数组一些奇奇怪怪的构造知识：
 A，x[0]=y;//二维数组赋值给一维数组，这显然是错的。
 B,y[0]=x;//y[0]是一个一维数组，x也是，所以可以把一维数组赋值给一维数组。对的。
 C,y[0][0]=x;//一维数组不能赋值给二维数组中的指定的某个值。这是错误的。

 D,x[0][0]=y;//x[0][0]压根都不是二维数组怎么可能这样去表示呢，这是错的。
 E,y[0][0]=x[0];//一维数组中的某个值可以赋值给二维数组中的某个值。对的。
